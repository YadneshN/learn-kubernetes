# Exercises
1. Apply pod.yaml and check pod status.
2. Scale the deployment to 3 replicas: kubectl scale deployment hello-deployment --replicas=3
3. Delete the pod and observe ReplicaSet recreating it.
